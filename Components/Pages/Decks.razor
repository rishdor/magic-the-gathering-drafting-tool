@page "/decks"
@rendermode InteractiveServer
@inject NavigationManager _nav

<PageTitle>Decks</PageTitle>

<h1>Decks</h1>

<ul>
@foreach (var deck in decks)
{
    <li>@deck.DeckName</li>
}
</ul>

<button class="btn btn-primary" onclick='@(()=>_nav.NavigateTo("/setup"))'>Create a new deck.</button>